---
import { ViewTransitions } from "astro:transitions";
import "@fontsource-variable/jost";

import Menu from "@/modules/navbar/Menu/Menu.astro";
import Seo from "@/components/Seo/Seo.astro";
// import NoiseBackground from "@/components/NoiseBackground/NoiseBackground.astro";

interface Props {
  title: string;
}

const { title } = Astro.props;

const { currentLocale } = Astro;
---

<!doctype html>
<html lang={currentLocale}>
  <head>
    <Seo title={title} />
    <ViewTransitions />
  </head>
  <body class="mb-10 flex flex-col bg-secondary md:flex-row">
    <!-- <NoiseBackground /> -->
    <!--
     `    ``               `
      ``  _ `      `       ``
     `   |_| `  `` ``    `  `
    ``  -___-_` `   ` --------------
  ``   /      )      | This is fine |`
 `____/| (0) (0)_()  |/-------------  `
/|   | |   ^____)      ``      ``
||   |_|    \_//     Uɔ````   `` ``
||    || |    |    ========`  ``  ``
||    || |    |      ||     ``   `
||     \\_\   |\     ||   ```    `
=========||====||    ||  ``       `
  || ||   \Ɔ || \Ɔ   ||   ``    ``
  || ||      ||      ||  `     ``
    -->
    <section
      class="flex flex-shrink-0 flex-col justify-between border-b-2 border-primary/20 p-8 md:fixed md:left-0 md:top-0 md:h-screen md:min-h-screen md:w-full md:max-w-56 md:border-b-0 md:border-r-2 md:pb-0 md:pl-4 md:pr-4 md:pt-4"
    >
      <Menu />
    </section>

    <main
      class="mx-auto mt-10 flex w-full max-w-screen-lg flex-col px-4 sm:px-6 md:ml-64 md:mt-20 lg:px-8"
    >
      <slot />
    </main>
  </body>
</html>

<style is:global>
  :root {
    --color-primary: 5, 20, 35;
    --color-secondary: 255, 250, 247;
    --background-color: --color-primary;
  }

  .dark {
    --color-primary: 255, 250, 247;
    --color-secondary: 5, 20, 35;
  }

  html {
    font-family: "Jost Variable", system-ui, sans-serif;
    color: rgb(255, 36, 65);
  }
</style>
