---
interface Props {
  boxTitle: string;
}

const { boxTitle } = Astro.props;
---

<aside class="w-full max-w-full md:w-56">
  <div class="flex flex-col rounded-lg border-2 p-4">
    <h3 class="mx-auto mb-4 text-2xl">{boxTitle}</h3>

    <slot />
  </div>
</aside>
