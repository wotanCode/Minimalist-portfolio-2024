---
interface Props {
  sectionTitle: string;
}

const { sectionTitle } = Astro.props;
---

<div class="flex flex-col justify-between text-primary">
  <h2 class="mb-4 text-2xl">{sectionTitle}</h2>

  <slot />
</div>
